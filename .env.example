# Copy to .env and fill in. Never commit .env.
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# RAG + cache. Local Redis by default (brew services start redis).
REDIS_URL=redis://localhost:6379/0
